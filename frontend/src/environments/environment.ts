export const environment = {
    api:{
        baseUrl: "https://localhost/",
        endpoints: {
            user:{
                login:"http://localhost:8080/api/v1/login_check"
            }
        }
    }
}